import React from "react";
import ReactDOM from "react-dom";

const myFirstElement = <h1>My first simple react application</h1>;

ReactDOM.createRoot(document.getElementById("root")).render(myFirstElement);
